// import type jwt, { VerifyOptions } from "jsonwebtoken";
// import { JWT_SECRET } from "../constants/env";

// export const verifyToken = (token: string, options?: VerifyOptions & {secret: string}) => {
//     const {...verifyOpts, secret = JWT_SECRET} = options || {}
//     try {
//         const payload = jwt.verify(
//             token, secret, {
//                 ...defaults,
//             }
//         )
//     }

// }